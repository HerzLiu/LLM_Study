---
tags: [Claude-Code, 附录, 源码地图, 导航]
---

# 附录 · 源码地图

⬅ [00-总览](../00-%E6%80%BB%E8%A7%88.md)

> 🗺️ Claude Code 源码导航。看哪一章去翻哪个文件。

---

## 📦 源码位置

```
~/llm-study/_source/claude-code-source-code-main/
```

**统计**：~1884 文件 / ~512K 行 / TypeScript

---

## 🗂️ 顶层目录

```
claude-code-source-code-main/
├── README_CN.md             ⭐ 必读：源码作者写的总览
├── QUICKSTART.md            构建说明
├── package.json
├── docs/                    ⭐ 5 篇深度分析（中英双语）
│   ├── zh/
│   │   ├── 01-遥测与隐私分析.md
│   │   ├── 02-隐藏功能与模型代号.md
│   │   ├── 03-卧底模式分析.md
│   │   ├── 04-远程控制与紧急开关.md
│   │   └── 05-未来路线图.md
│   └── en/                  英文版同上
├── src/                     ⭐ 主源码（1884 文件）
├── stubs/                   Bun 编译时内建函数 stub
├── scripts/                 构建脚本
├── tools/                   外部工具（非内置工具，是构建工具）
├── types/                   类型定义
├── utils/                   工具函数
└── vendor/                  原生模块源码 stub
```

---

## 🏗 `src/` 内部地图

```
src/
├── main.tsx              ⭐ CLI 入口（4683 行，React/Ink）
├── QueryEngine.ts        ⭐ SDK/无头模式入口（1295 行）
├── query.ts              ⭐⭐ 主 Agent 循环（1729 行，最核心）
├── Tool.ts               ⭐ 工具接口定义（792 行）
├── Task.ts               任务/SubAgent 基类（125 行）
├── tools.ts              工具注册与过滤（389 行）
├── commands.ts           ~80 个斜杠命令
├── context.ts            用户输入上下文
├── cost-tracker.ts       API 成本累积
├── setup.ts              首次运行设置
│
├── assistant/            KAIROS 助手模式（feature-gated）
├── bridge/               Claude Desktop / 远程桥接
├── buddy/                Buddy 系统通知
├── cli/                  CLI 基础设施
├── commands/             ~80 个斜杠命令
├── components/           React/Ink 终端 UI
├── constants/            ⭐ 常量、prompts、配置
├── context/              上下文相关
├── coordinator/          多代理协调
├── entrypoints/          应用入口
├── hooks/                ⭐ React hooks（包含权限）
├── ink/                  Ink 框架封装
├── keybindings/          键位
├── memdir/               长期记忆
├── migrations/           升级迁移
├── outputStyles/         输出风格
├── plugins/              插件
├── query/                查询相关
├── remote/               远程
├── schemas/              schema 定义
├── screens/              终端"屏幕"
├── server/               服务端
├── services/             ⭐ 业务逻辑层
├── skills/               Skill 系统
├── state/                应用状态
├── tasks/                任务实现
├── tools/                ⭐⭐ 40+ 工具实现
├── types/                类型定义
├── upstreamproxy/        上游代理
├── utils/                ⭐ 工具函数（最大目录）
├── vim/                  Vim 键位模式
└── voice/                语音模式
```

---

## 📍 按章节定位

### 第 1 章：入门
- `README_CN.md` —— 总览
- `package.json` —— 项目元信息

### 第 2 章：Agent Loop
- ⭐ `src/query.ts` —— 主循环（1729 行）
  - `queryLoop()` 第 241 行
  - `while (true)` 第 307 行
  - `toolUseBlocks.length === 0` 判断 第 557 行
  - `runTools` 调用 第 1382 行
  - `maxTurns` 检查 第 1705 行
  - 循环结束 第 1728 行
- `src/QueryEngine.ts` —— SDK 入口
- `src/services/tools/StreamingToolExecutor.ts` —— 流式执行
- `src/services/tools/toolOrchestration.ts` —— 批量执行

### 第 3 章：工具系统
- ⭐ `src/Tool.ts` —— Tool 接口（第 362 行）
- `src/tools.ts` —— 注册与过滤
- ⭐ `src/tools/FileReadTool/` —— case study
  - `FileReadTool.ts:227` —— inputSchema
  - `FileReadTool.ts:337` —— buildTool
  - `prompt.ts` —— 给 LLM 的说明书
- 其他工具：`src/tools/<ToolName>/`

### 第 4 章：上下文管理
- ⭐ `src/services/compact/` —— 11 个文件
  - `autoCompact.ts` —— 触发判断 + 主流程
  - `compact.ts` —— 实际压缩
  - `prompt.ts` —— 压缩 prompt（含 `NO_TOOLS_PREAMBLE`）
  - `microCompact.ts` —— 微压缩
- ⭐ `src/tools/TodoWriteTool/prompt.ts` —— TodoWrite 设计
- `src/utils/tokens.ts` —— token 计数
- `src/utils/context.ts` —— context window 配置
- `src/utils/attachments.ts` —— 附件机制
- `src/memdir/` —— 长期记忆

### 第 5 章：System Prompt
- ⭐ `src/constants/prompts.ts` —— 主拼装器（914 行）
  - `getSystemPrompt()` 第 444 行
  - `getSimpleDoingTasksSection()` 第 199 行
  - `getActionsSection()` 第 255 行
  - `getUsingYourToolsSection()` 第 269 行
  - `getSimpleToneAndStyleSection()` 第 430 行
- `src/constants/system.ts` —— 身份前缀
- `src/constants/systemPromptSections.ts` —— 缓存分段管理
- `src/constants/outputStyles.ts` —— 输出风格

### 第 6 章：多 Agent
- ⭐ `src/tools/AgentTool/` —— 15 个文件
  - `AgentTool.tsx` —— 工具实现
  - `prompt.ts` —— 给主 Agent 看的"如何用 SubAgent"
  - `runAgent.ts` —— 派生执行
  - `forkSubagent.ts` —— Fork 实现
- ⭐ `src/tools/AgentTool/built-in/` —— 6 个内置 SubAgent
  - `exploreAgent.ts` —— READ-ONLY 搜索专家
  - `planAgent.ts` —— 规划
  - `verificationAgent.ts` —— 验证
  - `generalPurposeAgent.ts` —— 通用
- `src/Task.ts` + `src/tasks/` —— 任务类型

### 第 7 章：安全与权限
- ⭐ `src/utils/permissions/` —— 24 个文件
  - `PermissionMode.ts` —— 5 种模式
  - `PermissionResult.ts` —— 结果类型
  - `dangerousPatterns.ts` —— 危险解释器清单
  - `permissionRuleParser.ts` —— 规则解析
  - `shellRuleMatching.ts` —— shell 命令匹配
  - `bypassPermissionsKillswitch.ts` —— 远程开关
- ⭐ `src/tools/BashTool/bashSecurity.ts` —— **最硬核的安全核心**
- `src/hooks/useCanUseTool.tsx` —— 工具执行前拦截
- `src/hooks/toolPermission/` —— 权限处理器
- `vendor/sandbox/` + `node_modules/@anthropic-ai/sandbox-runtime/` —— 物理沙箱

### 第 8 章：MiniCC
- `~/llm-study/minicc/minicc.py` —— Python 实现

---

## 🔍 怎么用这份地图

### 场景 1：想看某个机制的真源码
1. 找到对应章节
2. 查这份地图对应文件
3. 用 grep / IDE 跳转

### 场景 2：想读完整源码（推荐顺序）
```
1. README_CN.md（先看总览）
2. src/query.ts（必读）
3. src/Tool.ts（必读）
4. src/tools/FileReadTool/（一个完整工具）
5. src/constants/prompts.ts（system prompt）
6. src/tools/AgentTool/built-in/exploreAgent.ts（SubAgent）
7. src/utils/permissions/（权限）
8. docs/zh/01-遥测与隐私分析.md（深度分析报告）
```

### 场景 3：搜索某个关键词
```bash
cd ~/llm-study/_source/claude-code-source-code-main
grep -rln "你要找的字符串" src/
```

---

## 📚 推荐先读 5 个文件

| # | 文件 | 为啥 |
|---|---|---|
| 1 | `README_CN.md` | 作者写的总览，最准 |
| 2 | `src/query.ts` 第 219-340 行 | 主循环精华 |
| 3 | `src/Tool.ts` 第 362-470 行 | Tool 接口定义 |
| 4 | `src/tools/AgentTool/built-in/exploreAgent.ts` | SubAgent 设计样板 |
| 5 | `src/constants/prompts.ts` 第 199-253 行 | Doing tasks 金句 |

---

## 🔗 返回

⬅ [00-总览](../00-%E6%80%BB%E8%A7%88.md)
